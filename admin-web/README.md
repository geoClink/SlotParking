SlotParking Admin Web (React)

Quick start:

1) Install dependencies

   npm install

2) Start dev server

   npm start

3) The app will assume the admin server is at http://localhost:4001 and uses a saved admin token (default dev-token-1234). You can change this in the login screen.
